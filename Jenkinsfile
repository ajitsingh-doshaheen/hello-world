pipeline {
    //agent { dockerfile true }
    agent any
    stages {
        stage('Test') {
            steps {
                echo "you are looking at the pipeline steps"
            }
        }
    }
}
